# Shared runtime deps for services/ (scanner, pricing, cur, copilot, rules),
# used both standalone and transitively by apps/api (see apps/api/requirements.txt).
boto3>=1.34.0
anthropic>=0.40.0
stripe>=10.0.0
requests>=2.31.0
